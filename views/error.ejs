<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs")%>
    <style>
        .error {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 2em;
            max-width: 500px;
            height: 50vh;
            margin: 0 auto;
        }
        .error h1 {
            display: flex;
            align-items: center;
            color: #eee;
        }

        .error p {
            text-align: center;
            font-weight: 600;
            color: #ddd;
            border-top: 2px solid #ddd;
            padding-top: 0.5em;
            width: 100%;
        }

        .error button {
            background: #ff0000;
            color: #fff;
            box-shadow: none !important;
            font-size: 1.2em;
            width: 100%;
            border-radius: 10px;
            padding: 0.5em;
        }

        .error a {
            margin-top: 1em;
            width: 80%;
        }
    </style>
    <body>
        <%- include("./partials/nav.ejs")%>
        <div class="error">
            <h1><img src="images/error.png" alt="Error" /> &nbsp; Not Found</h1>
            <p>This Page is not available</p>
            <a href="/"><button>Go To Feed</button></a>
        </div>
        <%- include("./partials/footer.ejs")%>
    </body>
</html>
